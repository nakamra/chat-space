%li.chat-message
  .chat-message__header
    %p.chat-message__name
      = message.user.name
    %p.chat-message__time
      = message.created_at.strftime('%m/%d %H:%M:%S')
  %p.chat-message__body
    = message.body
